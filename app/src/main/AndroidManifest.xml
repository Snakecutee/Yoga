<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yoga"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".model.AddCourseActivity"></activity>
        <activity android:name=".model.SearchActivity"></activity>
        <activity android:name=".index.HomeActivity" />
        <activity android:name=".model.EditCourseActivity"></activity>
        <activity android:name=".index.Classyoga"></activity>
        <activity android:name=".model.AddClassActivity" />
        <activity android:name=".model.EditClassActivity"></activity>
<!--        <activity android:name=".search.SearchActivity"></activity>-->

    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>